<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.bupt.demoapp.mapper.UserMapper">

    <select id="findByUsername" resultType="org.bupt.demoapp.entity.User">
        SELECT
            id,
            username,
            encrypted_password AS encryptedPassword
        FROM app_user
        WHERE username = #{username}
        LIMIT 1
    </select>

    <insert id="insert">
        INSERT INTO app_user (id, username, encrypted_password)
        VALUES (#{id}, #{username}, #{encryptedPassword})
    </insert>

</mapper>